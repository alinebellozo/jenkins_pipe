pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Oi para o Jenkins"'
                sh '''
                    echo "Nosso primeiro pipeline"
                    ls -la
                '''
            }
        }
    }
}